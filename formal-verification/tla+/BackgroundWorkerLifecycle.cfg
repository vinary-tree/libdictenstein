SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoOrphan
INVARIANT NoSelfJoin

PROPERTY Termination

\* The goal states (joined/exited, detached/exited) are terminal — reaching
\* either is success, not a deadlock — so disable TLC's deadlock check.
CHECK_DEADLOCK FALSE
