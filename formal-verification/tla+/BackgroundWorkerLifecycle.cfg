SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoOrphan

PROPERTY Termination

\* The goal state (owner = "joined", worker = "exited") is terminal — reaching
\* it is success, not a deadlock — so disable TLC's deadlock check.
CHECK_DEADLOCK FALSE
