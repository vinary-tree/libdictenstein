\* NEGATIVE CONTROL: an eager coordinator lookup happens before slot
\* classification. A resident completion therefore violates the zero-acquisition
\* hot-path invariant.
CONSTANTS
    USE_DELAYED_ACQUISITION = FALSE
    MaxRoot = 4
    MaxGeneration = 3

SPECIFICATION Spec

INVARIANT ResidentCompletionNeverAcquires

CHECK_DEADLOCK FALSE
