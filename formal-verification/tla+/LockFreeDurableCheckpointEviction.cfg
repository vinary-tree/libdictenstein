\* EVICTION-ON durable checkpoint, correct design: checkpoint_lsn = committed
\* WATERMARK. ALL invariants hold — the no-lost-write headline re-derived under
\* the added registry-publication + eviction actions, plus the two new
\* registry-safety invariants.
CONSTANTS
    Writers = {w1, w2}
    Lsns = {1, 2, 3}
    NoLsn = 0
    USE_WATERMARK = TRUE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT DurablePrefix
INVARIANT ImmutableSnapshotIsClosed
INVARIANT CaptureEqualsPublishFrontier
INVARIANT NoLostWriteUnderLockFreeCommit
INVARIANT RecoveredNeverInventsState
INVARIANT RegistryPointsAtDurableWatermark
INVARIANT EvictionTouchesOnlyDurable

CHECK_DEADLOCK FALSE
