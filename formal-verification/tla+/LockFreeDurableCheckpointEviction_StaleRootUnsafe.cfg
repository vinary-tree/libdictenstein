\* NEGATIVE control: publication that ignores the captured root pair must
\* violate exact root/catalog agreement.
CONSTANTS
    Writers = {w1, w2}
    Lsns = {1, 2, 3}
    NoLsn = 0
    Generations = {g1, g2}
    NoGeneration = NoGeneration
    USE_WATERMARK = TRUE
    UnsafeSemanticPreservesBinding = FALSE
    UnsafePublishIgnoresRoot = TRUE
    UnsafePublishBeforeStamp = FALSE
    UnsafeExactUseIgnoresRoot = FALSE
    UnsafeRecoveryUsesDetached = FALSE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT ExactRootRegistryAgreement

CHECK_DEADLOCK FALSE
