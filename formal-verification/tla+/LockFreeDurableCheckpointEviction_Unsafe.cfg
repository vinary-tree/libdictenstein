\* NEGATIVE control (eviction-ON): checkpoint_lsn = APPENDED frontier (not the
\* committed watermark). TLC MUST report a NoLostWriteUnderLockFreeCommit
\* violation — the SAME GAP_LEDGER #41 losing trace the base spec's `_Unsafe.cfg`
\* exhibits (a write appended-before-capture but committed-after-capture, archived
\* out of recovery's reach), now under the registry-publication + eviction actions
\* too. This proves the committed-watermark choice remains REQUIRED with eviction
\* on: publishing the registry does not rescue an unsafe checkpoint_lsn.
CONSTANTS
    Writers = {w1, w2}
    Lsns = {1, 2, 3}
    NoLsn = 0
    USE_WATERMARK = FALSE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT NoLostWriteUnderLockFreeCommit

CHECK_DEADLOCK FALSE
