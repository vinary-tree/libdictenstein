CONSTANT
    Revisions = {r0, r1, r2, r3}
    \* A single generation excludes rollover so the required counterexample is
    \* specifically delayed D1 fault helper -> completed inverse D2 eviction.
    Generations = {g0}
    Owners = {o0}
    Words = {w0}
    UnsafeUnfencedWordWrite = TRUE
    UnsafeEarlyFrontier = FALSE
    UnsafeRetirementWithoutFence = FALSE
    UnsafeCatalogAuthorizes = FALSE
    UnsafePublishWithoutStamp = FALSE

SPECIFICATION Spec
INVARIANT MaterializedResidencyMatchesPublishedRoot
CHECK_DEADLOCK FALSE
