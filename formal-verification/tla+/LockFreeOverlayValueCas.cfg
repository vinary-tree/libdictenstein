\* CONDITIONAL/RECOMPUTED value-write PHANTOM-SAFETY (C2: compare_and_swap + the merge
\* per-key CAS-retry loop), correct design: a refused write's already-durable Upsert
\* record is left UNRANKED (USE_BURN_ON_LOSS = TRUE) and dropped on an Overlay-regime
\* reopen, so a crash-recover reconstructs EXACTLY the live published state. ALL
\* invariants hold — NoPhantomConditionalWrite (recov = committed) +
\* NoLostConditionalWrite. Bounded: Terms = {a, b}, Vals = {1, 2}.
CONSTANTS
    Terms = {a, b}
    Vals = {1, 2}
    USE_BURN_ON_LOSS = TRUE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT NoPhantomConditionalWrite
INVARIANT NoLostConditionalWrite

CHECK_DEADLOCK FALSE
