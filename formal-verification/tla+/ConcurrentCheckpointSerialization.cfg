\* F3 / NF-3: the checkpoint_lock design (USE_LOCK = TRUE). All invariants HOLD —
\* serializing concurrent checkpoints prevents a torn block-0 descriptor.
CONSTANTS
    Checkpointers = {c1, c2}
    USE_LOCK = TRUE

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT NoTornDescriptor
INVARIANT MutualExclusion

CHECK_DEADLOCK FALSE
