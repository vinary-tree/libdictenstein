\* NEGATIVE control: checkpoint_lsn = APPENDED frontier (not the committed
\* watermark). TLC MUST report a NoLostWriteUnderLockFreeCommit violation — the
\* losing trace is a write appended-before-capture but committed-after-capture,
\* archived out of recovery's reach (the GAP_LEDGER #41 footgun reborn under
\* lock-free out-of-order commit). This proves the watermark choice is required.
CONSTANTS
    Writers = {w1, w2}
    Lsns = {1, 2, 3}
    NoLsn = 0
    USE_WATERMARK = FALSE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT NoLostWriteUnderLockFreeCommit

CHECK_DEADLOCK FALSE
