\* NEGATIVE CONTROL: allocation occurs after generation 1 is planned, a
\* generation-2 registry replaces it, and copied residency is accepted without
\* revalidating the plan generation under the second read lock.
CONSTANTS
    USE_EXACT_CLOSURE = TRUE
    USE_ANCESTOR_EXECUTION = TRUE
    REVALIDATE_SNAPSHOT = FALSE
    TargetBytes = 2
    CandidateCap = 3

SPECIFICATION Spec

INVARIANT TypeOK
INVARIANT AcceptedSnapshotsWereRevalidated

CHECK_DEADLOCK FALSE
