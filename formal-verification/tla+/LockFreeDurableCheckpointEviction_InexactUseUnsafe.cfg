\* NEGATIVE control: an exact operation authorized by a stale registry rather
\* than the current root pair must violate NoInexactUse.
CONSTANTS
    Writers = {w1, w2}
    Lsns = {1, 2, 3}
    NoLsn = 0
    Generations = {g1, g2}
    NoGeneration = NoGeneration
    USE_WATERMARK = TRUE
    UnsafeSemanticPreservesBinding = FALSE
    UnsafePublishIgnoresRoot = FALSE
    UnsafePublishBeforeStamp = FALSE
    UnsafeExactUseIgnoresRoot = TRUE
    UnsafeRecoveryUsesDetached = FALSE

SPECIFICATION Spec

INVARIANT TypeInvariant
INVARIANT NoInexactUse

CHECK_DEADLOCK FALSE
