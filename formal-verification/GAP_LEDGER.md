# Formal Verification Gap Ledger

Updated: 2026-05-25

This ledger records the current proof/modeling boundary for the formal
verification artifacts. "Closed" means the repository now contains a checked
Rocq proof or a TLC-checked bounded model. "Scoped" means the claim is
intentionally narrower than the production system.

## Closed In This Update

| Area | Artifact | Status |
|------|----------|--------|
| Bucket model | `rocq/Model/Bucket.v` | Added `wf_sorted_bucket`, empty sorted-bucket proof, and a nontrivial `binary_search_in_bounds` theorem. |
| Checked bucket construction | `rocq/Spec/ARTrieSpec.v` | Added `canonical_bucket_checked_wf`, proving successful checked bucket construction preserves `wf_bucket` under suffix uniqueness. |
| Structural obligations | `rocq/Invariants/StructuralInvariants.v`, `rocq/Proofs/StructuralPreservation.v` | Re-scoped named preservation obligations to successful checked operations and proved the empty checked-builder preservation case. |
| Binary-search partition | `rocq/Model/Bucket.v` | Added `binary_search_partition`: for sorted bucket entries, the returned lower-bound position partitions entries before the probe from entries at/after the probe. |
| Canonical entry normalization | `rocq/Spec/ARTrieSpec.v` | Added sorted/deduplicating checked-entry normalization, proved lookup preservation, and proved normalized entries induce sorted, duplicate-free canonical buckets. |
| Nonempty checked canonical structure | `rocq/Proofs/StructuralPreservation.v` | Proved successful nonempty checked canonical construction satisfies the full `structural_invariant`, including root child-count accuracy and bucket sortedness. |
| Checked insert/delete preservation | `rocq/Proofs/StructuralPreservation.v` | Proved `insert_preserves_structural_obligation` and `delete_preserves_structural_obligation` for successful checked operations. |
| Byzantine storage faults | `rocq/Proofs/ByzantineRecovery.v`, `tla+/ByzantineStorage.tla` | Added fail-closed proofs/models: recovery applies only committed and authenticated records. TLC passed with 11,059,201 generated states and 331,776 distinct states. |
| Byzantine quorum safety | `rocq/Model/HotStuff.v`, `rocq/Proofs/HotStuffSafety.v`, `tla+/HotStuffConsensus.tla` | Added a HotStuff/PBFT-style safety layer: 2f+1 quorums over 3f+1 replicas intersect, honest vote locks force prefix-compatible committed logs, and TLC passed the bounded one-Byzantine branch-conflict model with 17,991 generated states and 2,940 distinct states. |
| Certified reference boundary | `rocq/Proofs/CertifiedReference.v` | Added theorem wrappers for the proved `WFARTrie` reference interface and documented the TCB boundary. |
| Proof-carrying replay boundary | `rocq/Proofs/ProofCarryingExtraction.v`, `tests/persistent_artrie_formal_correspondence.rs` | Added a certified trace checker proof: valid certificates replay to the reference command-log semantics and invalid post-states are rejected. Rust correspondence tests cover valid replay and corrupt-certificate rejection. |
| Document transactions | `tla+/DocumentTransactions.tla` | Added bounded commit/abort/staged-write model. TLC passed with 39,205 generated states and 10,057 distinct states. |
| Async WAL/group commit | `tla+/AsyncWalGroupCommit.tla` | Added bounded pending/durable/group-queue model. TLC passed with 36 generated states and 19 distinct states. |
| Version lifecycle | `tla+/VersionLifecycle.tla` | Added bounded reader/retire/reclaim/durable model. TLC passed with 963 generated states and 177 distinct states. |
| Durability and reclamation frontier | `tla+/DurabilityFrontier.tla`, `tests/persistent_artrie_loom_correspondence.rs` | Added a bounded durable-prefix/checkpoint/recovery/VersionGc model. TLC passed with 176,038 generated states and 16,995 distinct states. Loom checks cover no-early group-commit acknowledgement, unique contiguous LSN publication, async WAL gap handling, checkpoint publication within the synced frontier, and reader-guarded reclamation. |
| Raw pointer ownership boundary | `tla+/PointerOwnership.tla`, `tests/persistent_artrie_formal_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Extended the bounded ownership/borrow model to cover disk slots, lazy-load candidates, winner publication, losing-candidate cleanup, node-map raw entries, unswizzle-to-disk, and drop transitions. TLC passed with 28,379 generated states and 4,463 distinct states. Rust checks cover char and vocab child removal, replacement, deep clone ownership, lazy-load loser reclamation, swizzled raw extraction only after confirmed in-memory state, and explicit unsafe `Send`/`Sync` contract surfaces. Optional Miri execution is wired behind `RUN_MIRI=1`. |
| Vocab persistence and eviction ownership | `tla+/VocabPersistenceOwnership.tla`, `src/persistent_vocab_artrie/mod.rs`, `tests/persistent_artrie_formal_correspondence.rs` | Added a bounded checkpoint/reopen/eviction model for stable vocab term indexes, reverse indexes, node-map liveness, and reverse-cache consistency. TLC passed with 83,605 generated states and 7,985 distinct states. Rust checks cover Unicode checkpoint/reopen bijection, duplicate insert stability after reopen, checkpoint/reopen `node_map`/parent-chain rebuild, parent-eviction rejection while children are resident, leaf-eviction `node_map` invalidation before drop, and sibling queries after leaf eviction. |
| Mmap block-storage synchronization | `tla+/MmapBlockStorage.tla` | Added bounded allocation/remap/access model for `MmapDiskManager`: block IDs are unique, published `file_size` never exceeds `mmapLen`, completed allocations are mapped before access, and successful reads/writes stay within the published mapping. TLC passed with 1,618,433 generated states and 540,928 distinct states. |
| Storage syscall outcome boundary | `tla+/StorageSyscallOutcome.tla`, `src/persistent_artrie_core/io_uring_disk_manager.rs`, `tests/persistent_artrie_formal_correspondence.rs` | Added a bounded model for full, short, error, interrupted, cancelled, and missing write/sync outcomes. Only a full write followed by successful sync advances the durable/reported/recovered prefix. TLC passed with 88 generated states and 42 distinct states. Rust checks cover io_uring completion helper fail-closed behavior and failed WAL segment fsync not advancing `global_synced_lsn`. Cached io_uring write paths re-mark affected cached blocks dirty on failed single, batched, and fixed-buffer writes. |
| io_uring fixed-buffer ownership | `tla+/IoUringFixedBufferOwnership.tla`, `src/persistent_artrie_core/{block_storage,buffer_manager,io_uring_disk_manager}.rs`, `tests/unsafe_boundary_contracts.rs` | Added a bounded fixed-buffer lifetime model covering valid registration, invalid-registration rejection, fixed I/O only while registered, unregister with no in-flight fixed operation, fallback standard I/O, and owner drop only after unregister. TLC passed with 2,329 generated states and 456 distinct states. Rust now enables `BufferManager` fixed I/O only when registration succeeds and the backend reports fixed-buffer capability, with regressions for default no-op registration and a fixed-capable storage double that exercises write-guard mutation, batched fixed flush, and unregister-before-owner-drop. |
| io_uring SQE/CQE lifecycle | `tla+/IoUringSqeCqeLifecycle.tla`, `src/persistent_artrie_core/io_uring_disk_manager.rs`, `.github/workflows/ci.yml` | Added a bounded submission/completion model covering one live buffer per submitted request, fixed requests only on registered buffers, at most one checked CQE per request, short/error completions failing closed, and temporary aligned buffers returning only after completion checking. TLC passed with 6,785 generated states and 1,984 distinct states. |
| Byte lock-free ARTrie linearizability | `tla+/LockFreeARTrieLinearizability.tla`, `tests/persistent_artrie_loom_correspondence.rs` | Added a bounded root-CAS/cache/contains/merge model for the byte lock-free overlay. TLC passed with 38,379 generated states and 7,593 distinct states. Loom schedule checks cover single-winner root CAS, duplicate insert linearization, insert-vs-contains visibility, merge snapshot behavior, and child-pointer Arc handoff. |
| Indexed lock-free overlays | `tla+/LockFreeIndexedOverlay.tla`, `tests/persistent_artrie_loom_correspondence.rs` | Added bounded char counter and vocab index-assignment models. Counter TLC passed with 7,681 generated states and 900 distinct states; vocabulary TLC passed with 1,659 generated states and 333 distinct states. Loom checks cover char same-key increments, create-vs-increment races, merge snapshots, vocab duplicate insert stability, distinct-term unique indices, sparse `next_index`, and cache/root/persistent agreement. |
| Rust correspondence harness | `tests/persistent_artrie_formal_correspondence.rs`, `tests/persistent_merge_correspondence.rs`, `tests/root_descriptor_reopen_correspondence.rs`, `tests/persistent_lazy_mutation_correspondence.rs`, `tests/persistent_wal_atomicity_correspondence.rs`, `tests/checkpoint_retention_correspondence.rs`, `scripts/verify-formal-correspondence.sh`, `.github/workflows/ci.yml` | Added executable checks for bucket ordering/search partitions, split/merge preservation, trie-vs-`BTreeMap` traces, deterministic large/reopen traces, document transaction visibility, WAL CRC fail-closed behavior, version-GC reader protection, group-commit LSN durability, persistent cursor pagination and batched/grouped/parallel merge equivalence, root descriptor/reopen fallback, persistent lazy mutation atomicity, persistent WAL write-atomicity, checkpoint/WAL retention, vocab checkpoint/reopen and eviction ownership, proof-carrying replay/corrupt-certificate rejection, end-to-end crash-prefix recovery, storage boundaries, unsafe inventory drift, TLA+ SANY, optional Miri, optional io_uring, optional TLC, and Loom schedule checks. Full script, including then-current TLC modules, passed on 2026-05-22; the no-TLC verification script passed on 2026-05-25 under explicit process caps. The Miri gate now includes the expanded swizzled raw-extraction, vocab reopen rebuild, vocab eviction query, and buffer-manager fixed-registration targets. CI runs the no-TLC harness, Miri-gated harness, io_uring-gated harness, and scheduled/manual TLC harness under explicit process memory caps. |
| BlockStorage correspondence | `tests/persistent_artrie_storage_correspondence.rs`, `src/persistent_artrie_core/{disk_manager,io_uring_disk_manager}.rs` | Added storage-boundary tests for concurrent mmap allocation uniqueness/accessibility, fail-closed sub-block range checks, header-checksum refresh on sync/reopen, `raw_ptr` out-of-block rejection, pure swizzled disk-pointer raw roundtrip, and optional io_uring range, fixed-buffer registration, and unregister-before-owner-drop checks. Mmap sub-block I/O rejects cross-block ranges, `sync()` refreshes the header checksum before flushing, and io_uring fixed-buffer registration now rejects null, short, or unaligned buffers. `BufferManager` no longer treats default no-op registration as fixed-buffer capability. io_uring cached writes now fail closed by re-marking affected cached blocks dirty when single, batched, or fixed-buffer writes fail. |
| WAL codec and torn-tail recovery | `tests/persistent_artrie_formal_correspondence.rs` | Added executable correspondence checks for all public WAL record variants: payload roundtrip, serialized-size accounting, truncated-payload rejection, invalid type rejection, durable-prefix preservation when a later payload is torn, and safe ignoring of a partial trailing record header. |
| End-to-end crash-prefix recovery | `tests/persistent_artrie_formal_correspondence.rs` | Added `PersistentARTrie::open` correspondence cases over real WAL bytes: header-only recovery, every complete record-boundary prefix, torn payload after a durable prefix, and transaction replay where committed records apply atomically while incomplete transactions are ignored. |
| On-disk parser rejection | `tests/persistent_artrie_formal_correspondence.rs` | Added byte-level checks for WAL header roundtrip/rejection and bucket page roundtrip/rejection, including bad magic, unsupported version, and invalid-size cases. |
| Group-commit LSN correspondence | `src/persistent_artrie_core/group_commit.rs`, `src/persistent_artrie_core/wal/{writer,async_writer}.rs` | Group commit now writes queued records with the LSN reserved and returned to callers, so the durable WAL record LSN matches the coordinator's published LSN. |
| WAL value replay correspondence | `src/persistent_artrie_char/{mmap_ctor,io_uring_ctor}.rs` | Direct open/replay now deserializes value-bearing insert records instead of replaying them as term-only inserts. |
| Unsafe-boundary executable checks | `formal-verification/UNSAFE_BOUNDARY.md`, `src/persistent_artrie_core/concurrency.rs`, `src/persistent_artrie*/nodes/atomic_ptr.rs`, `tests/persistent_artrie_formal_correspondence.rs` | Added an unsafe-boundary ledger, replaced the raw atomic `Arc` node slot with a lock-guarded `Arc` slot, made `OptimisticCell` serialize Rust-level data access with `RwLock`, and added tests for swizzled-pointer transitions, single-winner CAS, node-pointer refcount balance, concurrent optimistic writes, end-to-end torn-WAL reopen, and dictionary law traces. |
| Whole-crate unsafe inventory gate | `formal-verification/UNSAFE_INVENTORY.tsv`, `formal-verification/UNSAFE_CONTRACTS.tsv`, `scripts/verify-unsafe-boundary-inventory.sh`, `tests/unsafe_boundary_contracts.rs` | Added a source-derived unsafe inventory covering live unsafe blocks, unsafe functions, and unsafe impls under `src/**/*.rs`. The correspondence harness now fails on unsafe inventory drift, requires every unsafe inventory tag to resolve to a reviewed contract row, and default-feature tests exercise byte and Unicode SCDAWG handle `Send`/`Sync` contracts under concurrent read traversal. |
| Public dictionary law conformance | `rocq/Spec/DictionaryLawSpec.v`, `tests/dictionary_law_correspondence.rs`, `src/scdawg_core/inner.rs` | Added checked public laws for exact sets, mapped dictionaries, set-zippers, mutation replay, and bijective forward/reverse maps. The Rust correspondence target checks static/dynamic byte and Unicode backends, suffix substring semantics, bijective maps, persistent trie/vocab APIs, and caught/fixed SCDAWG exact value lookup for suffix-sharing terms. |
| DynamicDawg mutation and compaction preservation | `rocq/Spec/DynamicDawgMutationSpec.v`, `src/dawg_core.rs`, `src/dynamic_dawg*.rs`, `tests/dynamic_dawg_mutation_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for byte/Unicode DynamicDawg insert, insert-with-value, update-or-insert, remove, compact, minimize, extend, remove-many, return values, and map-domain consistency under the valued-domain premise. The Rust target caught and fixed value loss during compact rebuild plus shared-node mutation after minimization by preserving optional values and adding copy-on-write for shared mutation paths. The claim is semantic language/map preservation, not exact node-count optimality. |
| DynamicDawgU64 sequence semantics | `rocq/Spec/DynamicDawgU64Spec.v`, `src/dynamic_dawg_u64.rs`, `src/dynamic_dawg_u64_zipper.rs`, `tests/dynamic_dawg_u64_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for u64-sequence insert, insert-with-value, update-or-insert, remove, batch replay, compact/minimize semantic no-ops, string/f64 adapter refinement, iterator/zipper exactness, and bounded snapshot-concurrency boundaries. The Rust target caught and fixed `update_or_insert_sequence` overwriting existing values before update and `iter()` omitting the empty sequence when root also had children. The claim is semantic set/map/snapshot safety, not wait-free progress or canonical minimization. |
| Double-array trie construction/traversal | `rocq/Spec/DoubleArrayTrieSpec.v`, `tests/double_array_trie_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added generic BASE/CHECK DAT laws for byte and Unicode root-state conventions: transition parent checks, base-offset walks, child-edge soundness/completeness, duplicate term collapse, mapped last-write-wins normalization, lookup/domain refinement, and zipper-path traversal. The Rust target checks byte and Unicode DAT construction, values, node walks, child iteration, zippers, sorted Unicode construction, and duplicate normalization. |
| Zipper/query-language conformance | `rocq/Spec/ZipperLanguageSpec.v`, `tests/zipper_language_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added backend-neutral traversal-language laws for zipper descent, child iteration, finality, valued lookup, prefix/excluding filters, and set/value-diff combinators. The Rust target checks byte and Unicode zippers, valued iteration, set combinators, prefix/excluding filters, value diffs, suffix automaton substring languages, SCDAWG exact/substring queries, and persistent byte/char zippers. |
| Valued set-combinator merge semantics | `rocq/Spec/ValuedSetCombinatorSpec.v`, `tests/valued_set_combinator_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added ordered value-fold laws for `UnionZipper`/`IntersectionZipper`: union domain is any-present, intersection domain is all-present, duplicate values fold left-to-right by dictionary order, `FirstWins`/`LastWins`/custom strategies refine the reference fold, lattice join/meet laws commute/associate under explicit lattice assumptions, and `lling-llang` semiring coverage is scoped to join=`plus` only. The Rust target checks byte DAT, Unicode DAT, DynamicDawg, numeric/bool/set lattice values, empty/disjoint domains, navigation-vs-iteration agreement, generated union cases, and feature-gated semiring join behavior. |
| Bloom filter no-false-negative lookup rejection | `rocq/Spec/BloomFilterSpec.v`, `src/bloom_filter.rs`, `src/dawg_core.rs`, `tests/bloom_filter_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added checked one-sided Bloom filter laws: inserted byte strings are never rejected, duplicate inserts preserve membership, later inserts preserve prior membership, `clear` rejects all payloads under the nonzero-hash invariant, string APIs refine byte APIs, false positives do not violate the safety property, and constructors normalize away zero hash counts. The Rust target mirrors the deterministic hash/bitset behavior, covers byte and Unicode string payloads, generated byte traces, parameter normalization, clear/reinsert traces, duplicate inserts, and Bloom-backed DynamicDawg lookup after rebuilds. |
| Persistent merge equivalence | `rocq/Spec/PersistentMergeSpec.v`, `tests/persistent_merge_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added checked cursor-pagination and merge laws proving exact pagination refines reference map merge for ordinary batched, arena-grouped batched, and parallel partitioned merge variants. The Rust target checks cursor pagination against `BTreeMap`, batched merge across batch sizes including zero-default normalization, grouped merge equivalence, and `parallel-merge` shared-trie correspondence. The claim is semantic map equality, not arena-locality or throughput optimality. |
| Persistent char prefix semantics | `rocq/Spec/PersistentPrefixSpec.v`, `tests/persistent_prefix_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for persistent char prefix filtering, empty-prefix behavior, missing-prefix emptiness, term/value domain agreement, ordinary removal, idempotence, and batched-removal equivalence. The Rust target checks `PersistentARTrieChar::iter_prefix*`, valued and arena-aware projections, ASCII/Unicode/empty prefixes, duplicate value updates, ordinary and batched removal across batch sizes including zero, and sync/reopen persistence. The claim is semantic map equality, not traversal order, exact arena placement, or page-locality optimality. |
| PathMap/factory conformance | `rocq/Spec/PathMapFactorySpec.v`, `tests/pathmap_factory_correspondence.rs`, `src/pathmap.rs`, `src/pathmap_char.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for optional PathMap byte/char map mutation, value update, union, node-edge soundness/completeness, UTF-8-backed character traversal, and factory backend-tag preservation. The Rust target checks byte PathMap map/zipper traversal, PathMapChar Unicode sibling edges sharing UTF-8 prefixes, mapped mutation/union, and all factory backends under `pathmap-backend`. It caught and fixed missing `MutableDictionary` impls plus PathMapChar edge enumeration that collapsed siblings with the same leading UTF-8 byte. |
| Persistent relative encoding safety | `rocq/Spec/RelativeEncodingSpec.v`, `tests/relative_encoding_correspondence.rs`, `src/persistent_artrie/relative_encoding.rs`, `src/persistent_artrie_char/relative_encoding.rs`, `src/persistent_artrie_char/serialization_char.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for strict same-arena relative roundtrip, full-encoding fallback for forward same-arena children, fail-closed empty/truncated/odd/underflow decode rejection, sequential sibling overflow rejection, and verified dedup-cache hit/collision soundness. The Rust target checks byte and char relative APIs plus char v2 deserialization corruption handling. It caught and fixed child-after-parent saturation that decoded to the parent slot, panic-prone truncated full pointers/varints, unchecked odd relative tags, relative underflow, and sequential overflow fallback-to-first behavior. |
| Arena reservation and slot-directory integrity | `rocq/Spec/ArenaReservationSpec.v`, `tests/arena_manager_correspondence.rs`, `src/persistent_artrie/arena.rs`, `src/persistent_artrie/arena_manager.rs`, `src/persistent_artrie_char/arena_manager.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for slot allocation/read roundtrip, same-size update replacement, contiguous reservation ranges, exhausted-reservation rejection, fail-closed dirty-slot flushing, and load/reopen directory reconstruction. The Rust target checks byte and char arena managers, including partial dirty-slot update persistence through reopen. It caught and fixed byte arena missing same-size slot updates and defensive validity recovery, char updates not marking the slot-level dirty tracker, and char partial dirty-slot writes not refreshing V3 checksums before writing the persisted header. |
| Persistent deduplicating-arena soundness | `rocq/Spec/DedupArenaSpec.v`, `tests/dedup_arena_correspondence.rs`, `src/persistent_artrie/dedup.rs`, `src/persistent_artrie_char/dedup.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for verified hit soundness, stale or colliding cache entries allocating fresh slots, compatibility setter preservation of verified mode, legacy unverified-hit soundness only under an explicit collision-free premise, cache clear, batch take, and byte/char backend parity. The Rust target checks equal-payload reuse, stale-cache fail-closed allocation, verify-false compatibility behavior, direct allocation bypass, cache clear, and batch-local take semantics for byte and char dedup managers. It caught and fixed the public compatibility setter allowing safe callers to disable read-back verification and rely on hash/stable-slot assumptions. The claim is semantic cache-slot soundness, not xxHash quality, cryptographic collision resistance, or optimal space savings. |
| Root descriptor and reopen refinement | `rocq/Spec/RootDescriptorReopenSpec.v`, `tests/root_descriptor_reopen_correspondence.rs`, `src/persistent_artrie/{disk_load,mmap_ctor,io_uring_ctor}.rs`, `src/persistent_artrie_char/{disk_io,mmap_ctor,io_uring_ctor,query_api}.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for descriptor validity, unknown root-kind rejection, final-flag validation, empty-payload constraints, arena-count bounds, checkpoint-threshold trust only after a valid loaded root, WAL replay from zero after descriptor/load failure, lazy `try_*` error propagation, public fail-closed reads, and byte/char reopen parity. The Rust target checks byte and char checkpoint/reopen, malformed descriptors with checkpoint records, invalid arena counts, and checksum-valid lazy child corruption. It caught and fixed byte unknown-root descriptors being loaded as empty while a checkpoint could skip WAL records, char test-only root-load panics, invalid `arena_count` trust/unbounded loading, and public char lazy-read panic wrappers. The claim is semantic reopen/reference-map preservation, not recovery of checkpoint-only data after descriptor corruption once WAL history was intentionally truncated. |
| Persistent lazy mutation atomicity | `rocq/Spec/PersistentLazyMutationSpec.v`, `tests/persistent_lazy_mutation_correspondence.rs`, `src/persistent_artrie_char/{mutation_core,mutation_api}.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for lazy-load error rejection before WAL append, duplicate/no-op mutation non-append behavior, successful insert/value-insert/remove replay correspondence, and byte/char backend parity. The Rust target checks char lazy term insert, value insert, remove, duplicate insert, WAL length stability on error/no-op, and replay after successful lazy writes. It caught and fixed char lazy insert panics and WAL-before-failed-mutation divergence by adding preflight plus `try_*_no_wal` mutation primitives. The claim is semantic public mutation/WAL atomicity, not filesystem/kernel internals or recovery of already-corrupted checkpoint-only data. |
| Persistent WAL write-atomicity | `rocq/Spec/PersistentWalAtomicitySpec.v`, `tests/persistent_wal_atomicity_correspondence.rs`, `src/persistent_artrie/{mutation_core,mutation_api,atomic_ops,document_tx}.rs`, `src/persistent_artrie_char/{atomic_ops,batch_insert,document_tx}.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for serialization/preflight rejection before WAL append, WAL append failure before memory mutation, duplicate/no-op atomic write non-append behavior, successful replay correspondence, and document commit batch/CommitTx ordering. The Rust target checks byte and char value-write serialization failures, byte and char atomic upsert/get-or-insert/CAS fail-closed behavior, document commit fail-closed behavior, and replay after successful atomic writes. It caught and fixed byte insert/remove/batch/atomic mutation-before-WAL paths, `.ok()` value serialization that logged key-only records while mutating values, byte document commits mutating before durable `CommitTx`, and char document SETs applying before commit. The claim is the WAL abstraction boundary and deterministic serializer behavior, not kernel write ordering below WAL append/sync or arbitrary concurrent writer schedules. |
| Checkpoint/WAL retention safety | `rocq/Spec/PersistentCheckpointRetentionSpec.v`, `tests/checkpoint_retention_correspondence.rs`, `src/persistent_artrie_core/recovery.rs`, `src/persistent_artrie/mmap_ctor.rs`, `src/persistent_artrie_char/mmap_ctor.rs`, `scripts/verify-formal-correspondence.sh` | Added checked laws for valid checkpoint skip thresholds, invalid-checkpoint no-skip behavior, active WAL retention before corruption rebuild, archive/pending/active replay order, batch-then-remove replay, safe truncation premises, and byte/char backend parity. The Rust target checks byte and char corruption rebuild from retained active WAL tails, archived checkpoints plus active post-checkpoint records, batch inserts, and removes. It caught and fixed rebuild paths that ignored the active WAL after file corruption and byte/char rebuild replay paths that skipped batch/remove records. The claim is segment-retention/replay safety at the WAL abstraction boundary, not kernel write ordering below successful WAL sync or recovery from already-deleted WAL segments. |
| Substring candidate correctness | `rocq/Spec/SubstringSearchSpec.v`, `tests/substring_candidate_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added backend-neutral exact substring candidate laws for non-empty patterns: `contains_substring` iff a reference occurrence exists, `find_exact_substring` is sound/complete/duplicate-free, result positions are in bounds, and limited search is a prefix bounded by `limit`. The Rust target checks byte and Unicode SCDAWG results against exhaustive reference occurrence sets, including repeated and overlapping matches, duplicate-term suppression, limited-result prefixes, and explicit empty-pattern behavior. |
| SCDAWG occurrence construction | `rocq/Spec/ScdawgOccurrenceSpec.v`, `tests/scdawg_occurrence_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added construction-facing SCDAWG occurrence laws: forward traversal accepts exactly nonempty reference substrings, left-extension closure enumerates term-end occurrence witnesses, `locations` is sound/complete/duplicate-free, `freq` counts locations, and the model derives the existing `SubstringSearchLaws`. The Rust target checks byte and Unicode `find`, `freq`, `locations`, handle-based `freq_at`/`locations_at`, left-extension traversal, duplicate value updates, and repeated/overlapping occurrence sets. |
| Fuzzy candidate coverage | `rocq/Spec/FuzzyCandidateCoverageSpec.v`, `tests/fuzzy_candidate_coverage_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added WallBreaker-style candidate coverage laws: a `budget + 1` nonempty query-piece partition and at most `budget` damaged pieces imply at least one untouched exact substring candidate. The Rust target checks byte and Unicode SCDAWG candidate coverage for substitution, insertion, deletion, deterministic generated substitution matrices, and short-query scope boundaries. |
| Public serialization roundtrip | `rocq/Spec/SerializationRoundtripSpec.v`, `tests/serialization_correspondence.rs`, `tests/serialization_value_roundtrip.rs`, `tests/protobuf_compression_correspondence.rs`, `scripts/verify-formal-correspondence.sh` | Added backend-neutral serializer laws for term-only membership roundtrip, value-aware mapped lookup roundtrip, intentional legacy value dropping, gzip codec wrapping, protobuf V1/V2/DAT/suffix formats, and fail-closed malformed payloads. The Rust targets check Bincode/JSON/plaintext, byte and Unicode char backends, SCDAWG value paths, generated roundtrip cases, gzip-wrapped serializers, protobuf graph formats, delimiter-safe DAT protobuf terms, suffix-automaton source language replay, and malformed payload rejection under `--features serialization` and `--features "serialization protobuf compression"`. |

## Scoped Claims

| Claim | Scope |
|-------|-------|
| Byzantine fault handling | Storage/WAL records may be dropped, duplicated, or corrupted, and the bounded quorum model covers one-Byzantine HotStuff/PBFT-style safety. The model does not claim production Byzantine networking, liveness, malicious CPU execution, compromised cryptography, or arbitrary Rust memory corruption. |
| Certified compilation | The checked claim is for the Rocq reference map interface and proof-carrying trace checker boundary. It does not certify the Rust binary or LLVM/codegen pipeline. |
| Structural preservation | The sound target is successful checked construction. Raw total rebuilds can exceed bucket capacity and are not claimed to preserve structure for every input. |
| Persistent root descriptor/reopen | The checked claim covers descriptor publication, root-kind/final-flag/empty-payload validation, arena-count bounds, root-load failure fallback, checkpoint skip-threshold selection, and lazy-read fail-closed semantics. It does not claim recovery of checkpoint-only state after descriptor corruption once older WAL records have been intentionally truncated, nor does it prove kernel/filesystem internals below the modeled storage boundary. |
| Persistent lazy mutation | The checked claim covers public mutation preflight and WAL append ordering around lazy-load errors and no-op mutations. It does not prove arbitrary concurrent mutation schedules, kernel write ordering below the WAL abstraction, or correctness after pre-existing checkpoint corruption. |
| Persistent WAL write atomicity | The checked claim covers byte/char value serialization, preflight, WAL append, atomic write no-op behavior, replay correspondence, and document commit ordering at the WAL abstraction boundary. It does not prove nondeterministic serializers, arbitrary concurrent writer interleavings, kernel write ordering below successful WAL append/sync, or certified Rust compilation. |
| Checkpoint/WAL retention | The checked claim covers checkpoint skip-threshold safety, invalid checkpoint/root-load no-skip behavior, archive/pending/active WAL segment retention during corruption rebuild, batch/remove replay, and byte/char parity. It does not prove recovery after required WAL segments were externally deleted, arbitrary filesystem reordering below the modeled sync boundary, or certified Rust compilation. |
| Unsafe/concurrency verification | The current work adds executable unsafe-boundary checks for representative ARTrie pointer/concurrency primitives, char/vocab raw child-pointer ownership transfer, lazy swizzle-race cleanup, swizzled raw-extraction gating, vocab persistence/eviction node-map liveness, buffer-manager aligned-block mutation and fixed-buffer registration lifetime, unsafe `Send`/`Sync` contracts, storage-boundary checks for mmap allocation/remap safety, storage syscall outcome fail-closed durability, io_uring fixed-buffer registration, and io_uring SQE/CQE completion discipline, dedicated storage-outcome, fixed-buffer, and SQE/CQE lifecycle models, and bounded Loom/TLA+ exploration for byte publication, char/vocab indexed lock-free overlays, raw pointer ownership, and the durability/reclamation frontier. Full kernel/syscall internals proof and a completed Miri CI pass remain future work. |
| Fuzzy-search boundary | The checked claim covers SCDAWG exact occurrence construction, exact substring candidate generation, and WallBreaker-style candidate coverage inside libdictenstein. Levenshtein automaton/transducer edit-distance correctness remains upstream-owned by `liblevenshtein`. |

## Remaining Extension Scope

| Area | Boundary |
|------|----------------------|
| Production Byzantine consensus | Out of scope. The current proof/model covers quorum/log safety for a bounded HotStuff/PBFT-style model, not a production network protocol, pacemaker, view-change liveness proof, or cryptographic implementation. |
| Certified Rust compilation | Out of scope. The checked claim covers the Rocq reference/proof-carrying boundary plus executable Rust correspondence tests, not a certified Rust/LLVM/codegen pipeline. |
| Unsafe-boundary proof | Partially covered by executable ARTrie checks, the source-derived unsafe inventory and contract-tag gates, `PointerOwnership.tla`, `VocabPersistenceOwnership.tla`, `MmapBlockStorage.tla`, `StorageSyscallOutcome.tla`, `IoUringFixedBufferOwnership.tla`, `IoUringSqeCqeLifecycle.tla`, `LockFreeARTrieLinearizability.tla`, `LockFreeIndexedOverlay.tla`, `DurabilityFrontier.tla`, storage/loom correspondence tests, and `UNSAFE_BOUNDARY.md`. A mechanized proof for all persistence raw-pointer paths and the kernel/syscall side of io_uring remains out of scope. |
| Public dictionary laws | Covered for the public exact, mapped, Bloom filter no-false-negative, DynamicDawg mutation/compaction, double-array trie construction/traversal, zipper traversal, valued set-combinator merge, PathMap/factory, persistent cursor/merge, persistent char prefix, persistent root descriptor/reopen, persistent lazy mutation, persistent WAL write-atomicity, checkpoint/WAL retention, SCDAWG occurrence construction, exact substring candidate, fuzzy candidate coverage, serializer roundtrip, bijective, and persistent APIs exercised by `tests/dictionary_law_correspondence.rs`, `tests/bloom_filter_correspondence.rs`, `tests/dynamic_dawg_mutation_correspondence.rs`, `tests/double_array_trie_correspondence.rs`, `tests/zipper_language_correspondence.rs`, `tests/valued_set_combinator_correspondence.rs`, `tests/pathmap_factory_correspondence.rs`, `tests/persistent_merge_correspondence.rs`, `tests/persistent_prefix_correspondence.rs`, `tests/root_descriptor_reopen_correspondence.rs`, `tests/persistent_lazy_mutation_correspondence.rs`, `tests/persistent_wal_atomicity_correspondence.rs`, `tests/checkpoint_retention_correspondence.rs`, `tests/substring_candidate_correspondence.rs`, `tests/scdawg_occurrence_correspondence.rs`, `tests/fuzzy_candidate_coverage_correspondence.rs`, `tests/serialization_correspondence.rs`, and `tests/protobuf_compression_correspondence.rs`. The claim is semantic API conformance to set/map/mutation/Bloom-filter-rejection/bijection/DAT-traversal/valued-merge/PathMap-factory/persistent-merge/persistent-prefix/root-descriptor-reopen/persistent-lazy-mutation/persistent-WAL-atomicity/checkpoint-retention/SCDAWG-occurrence/substring-candidate/fuzzy-candidate/serialization laws, not a proof of performance, Bloom false-positive rates, hash quality, arena-locality optimality, optimal DAT packing, optimal/minimal automata size, gzip/prost internals, cross-language protobuf implementations, semiring `times` as a meet for arbitrary semirings, upstream PathMap internals, or Levenshtein transducer correctness. |
